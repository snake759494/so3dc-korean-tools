# Star Ocean 3 DC Korean Tools

『스타 오션 3 Till the End of Time Director's Cut』 일본판 PS2(Disc 1·2)의
숨겨진 tri-Ace 아카이브, SLZ 압축, `so3mclib` 메시지·폰트 형식을 분석하고
한국어 패치를 만드는 연구 도구입니다.

**Disc 1 + Disc 2 전체 한국어화** 릴리스입니다(`v1.2.0`). 게임 텍스트를 담은
mclib 컨테이너 전부(Disc 1 1,350개 / Disc 2 1,328개)를 대상으로 하며,
대사·화자, 메뉴, 아이템명·설명, 문장술·전투 스킬, 적 이름, 아이템
크리에이션, 시스템 다이얼로그를 포함합니다. 캐릭터 이름은 대사 참조뿐
아니라 파티·스테이터스 화면의 엔진 하드코딩 이름 테이블까지 한글화합니다.
이미지(텍스처)에 픽셀로 박혀 있던 일본어(초기 설정 설명문, 설치·뮤직 모드
안내, 디스크 교체·오류 메시지, 타이틀 데모 필살기 라벨)도 텍스트 패치와
동일한 NanumSquare Neo로 원본 글자 크기·위치·색에 맞춰 다시 그렸습니다.

두 디스크의 공통 텍스트(메뉴·아이템·전투 등)는 동일 번역을 공유하고,
디스크 2 고유 후반 스토리는 추가 번역했습니다.

> 이전 알파(`v0.1`~`v0.4`)는 부분 패치, `v1.0.0`~`v1.1.0`은 Disc 1
> 전용이었습니다. `v1.2.0`부터 두 디스크를 모두 다룹니다.

> **저장소 이전 안내 (2026-09-14)** — 이 저장소는 이전 계정(`snake7594`)이 삭제되면서
> 원격 저장소·릴리스가 함께 사라진 뒤, 작업 세션 기록에서 **복원**한 것입니다.
> 릴리스 패치(xdelta)는 원본 v1.2.1과 바이트 단위로 동일함을 확인했습니다. 소스
> 파일별 복원 신뢰도와 아직 재생성 중인 항목은
> [`RECOVERY_STATUS.md`](RECOVERY_STATUS.md)를 참고하세요. 그 문서의 "미복원"
> 항목이 남아 있는 동안에는 "소스에서 직접 빌드" 절차가 완전히 동작하지 않습니다.

## 커버리지

| 항목 | Disc 1 | Disc 2 |
|---|---:|---:|
| 대상 컨테이너(24×24 mclib) | 1,350 | 1,328 |
| 대상 메시지 | 152,926 | 153,015 |
| 디스크 발생 | 293,025 | 292,190 |
| 한국어 교체 | 152,695 (99.85%) | 152,794 (99.86%) |
| 용량 한계로 원문 유지 | 231 (0.15%) | 221 (0.14%) |

- 대사·화자, 메뉴, 아이템명·설명, 문장술·전투 스킬, 적 이름, 아이템
  크리에이션, 시스템 다이얼로그 포함
- 캐릭터 이름 한글화: 페이트·소피아·마리아·클리프·넬·알벨·로저·스프레·
  아드레이·미라쥬 (대사 참조 + 엔진 하드코딩 이름 테이블 3벌 + 글로벌 폰트
  이름용 21개 슬롯 교체, 런타임 변환기는 무수정) — 두 디스크 공통
- 두 디스크의 공통 텍스트는 동일 번역 재사용(고유 컨테이너의 96.3%가 두
  디스크에서 바이트 동일), 디스크 2 고유 후반 스토리 1,625개 유닛 추가 번역
- 스태프롤(32×32 폰트, 인명 위주)은 제외
- 용량 한계 미교체분은 해당 아카이브가 원본 섹터 할당을 초과해 재압축·
  재배열로도 수용 불가한 소수 메시지이며, 결정론적으로 최소한만 제외

한국어 글리프는 `NanumSquareNeo-cBd.ttf`로 24×24 셀, 22px, 2단계 명암으로
생성해 컨테이너별 로컬 아틀라스에 삽입합니다. 원본 ISO·아카이브·숨김 인덱스
크기는 불변으로 유지합니다.

## 지원 원본

- 게임: 일본판 Director's Cut (Disc 1 제품 코드 `SLPM-65438`)
- Disc 1: `4,689,854,464` bytes, SHA-256
  `95CC4E25AC71DE7C6263AA2E544910DE30667EA3BA62726CF4A019F24B038826`
- Disc 2: `4,685,955,072` bytes, SHA-256
  `349FF9443E3ACFD92045C5DFAD6D97EB51D2765B0781BCD59DCD424D81670288`

해시가 다른 덤프에는 릴리스 xdelta를 적용하지 마세요. 패처와 검증기는 지원
크기·해시·구조가 다르면 처리를 중단합니다.

## xdelta 적용

GitHub Releases에서 최신 릴리스 ZIP을 받아 풀고 디스크별로 적용합니다.

```powershell
xdelta3 -d -s "SO3_DC_Disc1_original.iso" `
  "SO3_DC_Disc1_Korean_Full_v1.2.1.xdelta" `
  "SO3_DC_Disc1_Korean_Full.iso"

xdelta3 -d -s "SO3_DC_Disc2_original.iso" `
  "SO3_DC_Disc2_Korean_Full_v1.2.1.xdelta" `
  "SO3_DC_Disc2_Korean_Full.iso"
```

- Disc 1 출력 ISO SHA-256:
  `E31911EF099F3BDB46928DEA9420F745B77A3FC34DDF3BA3A3F272EC2E1E01BA`
- Disc 2 출력 ISO SHA-256:
  `753FE9209A82EFAD27365D4E660F263D19448D166274A027B00C322C9C0C65FF`

반드시 적법하게 소유한 원본 디스크 덤프를 사용하세요. 출력 ISO, xdelta,
릴리스 ZIP의 SHA-256은 릴리스 번들의 `SHA256SUMS.txt` 및 각 버전의
`docs/releases/vX.Y.Z-verification.md`에 기록합니다.

## 소스에서 직접 빌드 (전체 재현)

이 저장소에는 패치를 처음부터 재현하는 데 필요한 **전체 파이프라인**이 들어
있습니다 — 언팩커, 패처·검증기, 최종 번역 DB(한국어 텍스트 45,569레코드),
패치 플랜, 용어집·스타일 가이드, 글리프 라벨, 이미지 리페인트 명세, 이름
패치 설정. 본인 소유의 원본 ISO와 나눔 글꼴만 준비하면 릴리스와 바이트
단위로 동일한 ISO를 직접 빌드할 수 있고, 번역을 수정한 자기만의 빌드도
만들 수 있습니다.

**[`docs/BUILD_FROM_SOURCE.md`](docs/BUILD_FROM_SOURCE.md)** 를 따라 하세요.

### 저장소 구성

```
so3_repack.py            SLZ 압축·해제, mclib, 숨김 인덱스 코어 라이브러리
unpacker/                So3Unpack (C#) — 숨김 아카이브 전체 언팩 + 매니페스트
tools/                   초기 연구·부분 패치 도구 (patch_hyda_dialogue 등)
work/full_ko/            디스크 1 전체 패치 파이프라인
  ├ so3_full_patch.py      범용 텍스트 패처 (플랜 → ISO)
  ├ so3_name_patch.py      엔진 이름 테이블·글로벌 폰트 패처
  ├ verify_full_iso.py     독립 검증기 (전 발생 역디코딩 대조)
  ├ patch_plan_full.json   최종 패치 플랜 (v1.2.1)
  ├ tr_out_v121/           최종 번역 DB (키+한국어만)
  ├ glossary_full.json / STYLE_GUIDE.md / control_sizes_full.json
  └ build_inventory.py 등  인벤토리·배치·플랜 생성 체인
work/full_ko_d2/         디스크 2 확장 (카탈로그 빌더, d2 플랜·번역·설정)
work/img_ko/, img_ko_d2/ FIS 텍스처 디코더·리페인터·적용기 + 번역 명세
work/font_ocr/           글리프 비트맵 SHA → 문자 라벨 (OCR 판독 결과)
automation/workflows/    대량 번역·라벨링에 쓴 Claude Code 워크플로 스크립트
docs/                    분석 문서·릴리스별 검증 기록
```

게임에서 추출한 데이터(원문 텍스트·리소스·픽셀)는 저장소에 없으며, 전부
본인의 ISO에서 로컬로 재생성됩니다(`.gitignore`가 실수 커밋도 차단).

## 이미지(텍스처) 패치

게임 텍스트 대부분은 mclib 글리프로 렌더되어 텍스트 패치가 처리하지만, 일부
UI는 일본어가 이미지(FIS 텍스처)에 픽셀로 그려져 있습니다. 두 디스크 모두
일본어가 들어간 텍스처를 한국어로 다시 그립니다.

- 초기 설정 항목 설명문(7줄), PlayStation BB Unit 설치 안내문, 뮤직 모드
  설명문, 디스크 교체·오류 시스템 메시지(DISC 1/2 교체·디스크 다름·읽기
  실패·확인 중·디스크 없음 — 동일 사본 3벌 전부), 타이틀 데모 전투 필살기
  라벨 4종. 두 디스크의 해당 텍스처는 바이트 동일이라 같은 리페인트를
  적용합니다(디스크 2는 이동된 ISO 위치 반영).
- 글꼴은 텍스트 패치와 동일한 NanumSquare Neo Bold. 원본 일본어의 글자
  크기·위치·색(CLUT 인덱스)에 맞춰 렌더링하고, 영문 그래픽·로고·버튼·캐릭터
  이미지 등 비대상은 원본 그대로 보존합니다.
- FIS는 tri-Ace GS/GIF DMA 텍스처 포맷(CLUT CSM1, PS2 알파, 4/8/24/32bpp,
  일부 8bpp-위장-32bpp 언스위즐)으로, 편집 전 원본 바이트 왕복 재현을 증명한
  뒤 캡션 영역만 다시 칠하고 SLZ로 재압축해 원본 할당 안에 수용합니다.
- 독립 검증: 텍스처 멤버 영역 밖 전 영역이 텍스트 패치본과 바이트 동일
  (텍스트 패치 완전 보존), 각 텍스처 FIS 헤더·CLUT 불변, SLZ 왕복 일치.

## 구현 개요

- 메시지 바이트코드 제어 opcode 36계열을 엔진 디스어셈블 3중 교차검증으로 전수
  확정해 전 메시지를 안전하게 재인코딩합니다(토큰화 Disc 1 183,065/183,065,
  Disc 2 183,403/183,403 — 두 디스크의 엔진 모듈·피연산자 테이블 동일 확인).
- 범용 패처가 임의 PK1 패키지·행·SLZ 체인·PACK 테이블·glyph0 컨테이너를 다루며,
  실패 시 fail-closed로 중단합니다. 원본 ISO·아카이브·숨김 인덱스 크기는 절대
  바뀌지 않습니다.
- SLZ mode-2 최적 파스 압축기(그리디 대비 평균 +2.85%)와 비트맵 최근접 이웃
  재배열로 컨테이너를 원본 섹터 할당 안에 재수용합니다. 이름 테이블이 든 엔진
  코드 모듈은 SLZ mode-3 최적 인코더로 재작성합니다.
- 자동 픽셀 폭 게이트가 8a80 확대 배율을 반영해 각 라인 폭을 원본 예산 이내로
  맞춥니다.

## 정적 검증

독립 검증기는 패처 로직을 공유하지 않고 별도 경로로 결과를 다시 분석합니다.

- 원본·출력 ISO 크기·SHA-256, 폰트 SHA-256 고정
- 숨겨진 tri-Ace 인덱스(6,144 엔트리) 원시 바이트 + 디코딩 결과 불변
- 변경 범위가 대상 아카이브(D1 1,006개 / D2 973개) + 이름 테이블 4개 영역 +
  이미지 텍스처 영역 안으로 한정, 그 밖 전 영역 바이트 동일
- 한국어 화자·본문 전 발생 역디코딩 정확 일치
  (Disc 1 293,025/293,025, Disc 2 292,190/292,190)
- 이벤트·페이지·마커·제어열 보존, 비대상 메시지·글리프 불변
- 각 컨테이너의 한글 글리프를 NanumSquare Neo로 재렌더해 아틀라스 일치
- 글로벌 폰트의 이름용 21개 슬롯만 교체, 나머지 271개·헤더 불변
- xdelta 3.0.11 왕복 복원 ISO가 출력 ISO와 byte-identical (양 디스크)

빌드별 수치·해시는
[`docs/releases/v1.2.0-verification.md`](docs/releases/v1.2.0-verification.md)에
기록합니다.

## 알려진 제약

- 대사창 폭: 자동 폭 게이트 적용 후에도 축약 불가한 곳이 소수 남습니다
  (Disc 1 7곳, Disc 2 3곳) — 런타임에 삽입되는 값(날짜·상점 수치·달성률·
  비밀번호 입력칸)이 들어가는 칸과, 고유명사(테트라제네시스 등)가 원본보다
  1글자 미만 넓은 경우입니다.
- 유니코드 대응이 없는 장식 기호·아이콘 글리프는 원본 비트맵을 그대로 보존합니다.
- 타이틀 데모 필살기 라벨(사진 배경)에는 옅은 인페인트 흔적이 남을 수 있습니다.
- 이름 패치 이전에 만든 구(舊) 세이브는 세이브/로드 목록에서 이름이 깨져
  보일 수 있습니다 — 게임에서 한 번 다시 저장하면 정상 갱신됩니다.
- 정적 검증은 양 디스크 모두 통과했고 실기 구동으로 초기 화면·대사·메뉴
  표시를 확인했으나, 30만여 발생 전부를 게임 진행 전 구간에서 하나하나
  확인한 것은 아닙니다. 이상 표시가 보이면 제보해 주세요.

## 글꼴

한국어 글리프는 NAVER·Sandoll이 제공하는 NanumSquare Neo Bold
(`NanumSquareNeo-cBd.ttf`, SIL Open Font License 1.1)로 생성합니다.

- 공식 빌드 글꼴 SHA-256:
  `4749FA5691157CF56A59D297B45E88894A646846048018CD7A4117FFB2869767`

폰트 파일 자체는 저장소나 릴리스에 넣지 않습니다. 저작권 고지와 라이선스 전문은
[`LICENSES/NanumSquareNeo-COPYRIGHT.txt`](LICENSES/NanumSquareNeo-COPYRIGHT.txt)와
[`LICENSES/NanumSquareNeo-OFL-1.1.txt`](LICENSES/NanumSquareNeo-OFL-1.1.txt)를
참고하세요.

## 이전 알파 릴리스

부분 패치 시절의 도구·문서는 저장소에 남아 있습니다. 하이다 653개 대사 패치는
[`docs/HYDA_653_KOREAN_PATCH.md`](docs/HYDA_653_KOREAN_PATCH.md)와
[`docs/releases/v0.4.0-alpha.1-verification.md`](docs/releases/v0.4.0-alpha.1-verification.md)를
참고하세요.

## 법적 고지

이 프로젝트는 비공식 팬 연구 프로젝트이며 tri-Ace, Square Enix, Sony와 관계가
없습니다. ISO, 게임 실행 파일, 추출 게임 데이터, BIOS, PCSX2 세이브스테이트와
TTF 폰트 파일은 저장소나 릴리스에 포함하지 않습니다. 반드시 적법하게 소유한
원본 디스크 덤프를 사용하세요.
